# ============================================================================
# Next.js production environment variables
# docker-compose.yml 의 web 서비스에서 사용됩니다.
# ============================================================================

# --- Build/runtime toggles --------------------------------------------------
NODE_ENV=production

# --- Auth metadata ----------------------------------------------------------
# 배포 도메인으로 교체 (예: https://app.example.com)
NEXTAUTH_URL=https://example.com
# 운영 배포 전 안전한 랜덤 시크릿으로 교체
NEXTAUTH_SECRET=please-change-me

# --- API endpoint wiring ----------------------------------------------------
# 브라우저 요청은 Nginx -> /api 로 라우팅 (상대 경로)
NEXT_PUBLIC_BACKEND_URL=/api
# Next.js 서버사이드 fetch 시 내부 네트워크 주소 사용
BACKEND_API_URL=http://api:8000
