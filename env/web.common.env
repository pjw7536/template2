# ==============================================================================
# Vite 웹 공통 환경 변수
# ------------------------------------------------------------------------------
# dev / OIDC dev / prod web 서비스가 함께 사용하는 브라우저 노출 설정입니다.
# VITE_* 값은 클라이언트 번들에 포함될 수 있으므로 secret을 넣지 않습니다.
# ==============================================================================

# --- Portal 외부 링크 ---------------------------------------------------------
VITE_PORTAL_PMX_URL=
VITE_PORTAL_MOSAIC_URL=
VITE_PORTAL_CONFLUENCE_URL=
VITE_DEFECT_SPIDER_URL=http://10.173.129.168:32600

# Work Hub 메뉴 노출은 환경별 override로 opt-in 합니다.
VITE_WORK_HUB_ENABLED=0
