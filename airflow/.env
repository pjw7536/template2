# Airflow 컨테이너에서 사용할 UID (호스트의 실제 UID를 쓰면 권한 문제 적음)
AIRFLOW_UID=50000

# Airflow 필수 키 (openssl 등으로 생성)
AIRFLOW_WEBSERVER_SECRET_KEY=replace-with-random-32bytes
AIRFLOW_FERNET_KEY=replace-with-fernet-key

# 기본 사용자 (airflow-init에서 사용)
_AIRFLOW_WWW_USER_USERNAME=airflow
_AIRFLOW_WWW_USER_PASSWORD=airflow

# 프로젝트 경로 (기본은 현재 디렉터리)
AIRFLOW_PROJ_DIR=.

# code-server 로그인 비밀번호
CODE_SERVER_PASSWORD=dkssud123!
