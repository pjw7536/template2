services:
  prometheus:
    image: repository.samsungds.net/proxy-docker-registry-1.docker.io/prom/prometheus:v2.55.1
    command:
      - --config.file=/etc/prometheus/prometheus.yml
      - --storage.tsdb.path=/prometheus
      - --storage.tsdb.retention.time=${PROMETHEUS_RETENTION_TIME:-15d}
      - --web.enable-lifecycle
    volumes:
      - ../monitoring/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus_data:/prometheus
    expose:
      - "9090"
    restart: unless-stopped
    networks:
      shared-net:
        aliases:
          - prometheus

  node-exporter:
    image: repository.samsungds.net/proxy-docker-registry-1.docker.io/prom/node-exporter:v1.8.2
    command:
      - --path.rootfs=/host
      - --collector.filesystem.mount-points-exclude=^/(sys|proc|dev|host|etc)($$|/)
    pid: host
    volumes:
      - /:/host:ro,rslave
    expose:
      - "9100"
    restart: unless-stopped
    networks:
      shared-net:
        aliases:
          - node-exporter

  cadvisor:
    image: repository.samsungds.net/proxy-docker-gcr.io/cadvisor/cadvisor:v0.49.1
    privileged: true
    devices:
      - /dev/kmsg:/dev/kmsg
    volumes:
      - /:/rootfs:ro
      - /var/run:/var/run:ro
      - /sys:/sys:ro
      - /var/lib/docker:/var/lib/docker:ro
      - /dev/disk:/dev/disk:ro
    expose:
      - "8080"
    restart: unless-stopped
    networks:
      shared-net:
        aliases:
          - cadvisor

  grafana:
    image: repository.samsungds.net/proxy-docker-registry-1.docker.io/grafana/grafana:11.5.2
    depends_on:
      - prometheus
    expose:
      - "3000"
    volumes:
      - grafana_data:/var/lib/grafana
      - ../monitoring/grafana/provisioning:/etc/grafana/provisioning:ro
      - ../monitoring/grafana/dashboards:/var/lib/grafana/dashboards:ro
    restart: unless-stopped
    networks:
      shared-net:
        aliases:
          - grafana

volumes:
  prometheus_data:
  grafana_data:

networks:
  shared-net:
    external: true
    name: shared-net
