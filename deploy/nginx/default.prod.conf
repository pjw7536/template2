# =========================
# WebSocket 업그레이드용 (유지)
# =========================
map $http_upgrade $connection_upgrade {
  default upgrade;
  ''      close;
}

# =========================
# 업스트림 (가독성 + 연결 유지)
# =========================
upstream web_upstream {
  server web:3000;
  keepalive 32;
}

upstream api_upstream {
  server api:8000;
  keepalive 32;
}

upstream airflow_upstream {
  server airflow-webserver:8080;
  keepalive 16;
}

upstream minio_upstream {
  server minio:9000;
  keepalive 16;
}

upstream minio_console_upstream {
  server minio:9001;
  keepalive 8;
}

upstream grafana_upstream {
  server grafana:3000;
  keepalive 8;
}

upstream tttm_spider_upstream {
  server ${TTTM_SPIDER_UPSTREAM};
  keepalive 8;
}

# =========================
# 1) HTTP -> HTTPS 리다이렉트
# =========================
server {
  listen 80;
  server_name stg.plane.samsungds.net;
  return 301 https://$host$request_uri;
}

# =========================
# 2) HTTPS 서버
# =========================
server {
  listen 443 ssl http2;
  server_name stg.plane.samsungds.net;

  # TLS 인증서 경로 (/etc/nginx/certs 로 호스트 마운트 필요)
  ssl_certificate     /etc/nginx/certs/fullchain.crt;
  ssl_certificate_key /etc/nginx/certs/server.key;

  # 최신 TLS 기본값
  ssl_protocols TLSv1.2 TLSv1.3;
  ssl_prefer_server_ciphers off;
  ssl_session_cache   shared:SSL:10m;
  ssl_session_timeout 1d;

  # HSTS(선택) - 운영 정책에 맞을 때만 includeSubDomains 유지
  add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;

  # 공통 성능/제한
  sendfile on;
  tcp_nopush on;
  tcp_nodelay on;
  types_hash_max_size 2048;

  # 업로드 여유 (Django 업로드/첨부 대비)
  client_max_body_size 50m;

  # =========================
  # 공통 프록시 기본값 (location에서 상속될 수 있으나,
  # Vite Host 차단 이슈 방지를 위해 각 location에 핵심 헤더를 "명시"함)
  # =========================
  proxy_http_version 1.1;
  proxy_redirect off;
  proxy_read_timeout 3600s;
  proxy_send_timeout 3600s;

  # =========================
  # (B) 정적 파일 직접 서빙 (/static/)
  # =========================
  location /static/ {
    alias /var/www/static/;
    access_log off;
    expires 30d;
    add_header Cache-Control "public";
    # add_header Cache-Control "public, max-age=2592000, immutable";
  }

  # =========================
  # (A) Django 백엔드 프록시
  # =========================

  # (A) API 프록시 (/api/) - /api 접두사 유지
  location = /api/v1/assistant/openwebui-chat/stream {
    proxy_pass http://api_upstream;
    proxy_http_version 1.1;
    proxy_set_header Connection "";
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_buffering off;
    proxy_cache off;
    proxy_read_timeout 180s;
  }

  location /api/ {
    proxy_pass http://api_upstream;

    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
  }

  # (A-1) 인증/콜백 URL 프록시 (/auth/)
  location /auth/ {
    proxy_pass http://api_upstream;

    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
  }

  # (A-3) Django Admin 프록시 (/admin/)
  location /admin/ {
    proxy_pass http://api_upstream;

    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
  }

  # =========================
  # (A-2) Airflow UI 프록시 (/airflow/)
  # =========================
  location /airflow/ {
    proxy_pass http://airflow_upstream;

    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;

    # WebSocket/스트리밍 대응
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $connection_upgrade;

    # (선택) 스트리밍/로그 테일링에서 버퍼링 이슈 있으면
    # proxy_buffering off;
  }

  # =========================
  # (A-4) MinIO 프록시 (/minio/)
  # =========================
  location = /minio {
    return 301 /minio/;
  }

  location /minio/ {
    proxy_pass http://minio_upstream/;

    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
  }

  # =========================
  # (A-5) MinIO 콘솔 프록시 (/minio/console/)
  # =========================
  location = /minio/console {
    return 301 /minio/console/;
  }

  location /minio/console/ {
    proxy_pass http://minio_console_upstream/;

    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-Forwarded-Prefix /minio/console;

    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $connection_upgrade;

    proxy_redirect ~^(/.*)$ /minio/console$1;
  }

  # =========================
  # (A-6) Grafana 프록시 (/grafana/)
  # - Django 세션 인증을 먼저 확인한 뒤 내부 Grafana로 전달
  # =========================
  location = /_grafana_auth {
    internal;
    proxy_pass http://api_upstream/api/v1/auth/me;
    proxy_pass_request_body off;

    proxy_set_header Content-Length "";
    proxy_set_header Cookie $http_cookie;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
  }

  location @grafana_login {
    return 302 /api/v1/auth/login?target=/grafana/;
  }

  location = /grafana {
    return 301 /grafana/;
  }

  location /grafana/ {
    auth_request /_grafana_auth;
    error_page 401 403 = @grafana_login;

    proxy_pass http://grafana_upstream;

    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-Forwarded-Prefix /grafana;

    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $connection_upgrade;
  }

  # =========================
  # (A-7) TTTM Spider 프록시 (/tttm-spider/)
  # - HTTPS 페이지에서 HTTP iframe을 직접 로드하지 않도록 같은 origin 경로로 감싼다
  # =========================
  location = /tttm-spider {
    return 301 /tttm-spider/;
  }

  location /tttm-spider/ {
    proxy_pass http://tttm_spider_upstream/;

    # upstream HTML/JS/CSS 안의 HTTP 실행 리소스를 same-origin 경로로 치환한다.
    proxy_set_header Accept-Encoding "";
    sub_filter_once off;
    sub_filter_types text/css text/javascript application/javascript application/x-javascript application/json;
    sub_filter "http://${TTTM_SPIDER_UPSTREAM}/" "/tttm-spider/";
    sub_filter "http://${TTTM_SPIDER_UPSTREAM}" "/tttm-spider";
    sub_filter "ws://${TTTM_SPIDER_UPSTREAM}/" "wss://$host/tttm-spider/";
    sub_filter "ws://${TTTM_SPIDER_UPSTREAM}" "wss://$host/tttm-spider";

    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-Forwarded-Prefix /tttm-spider;

    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $connection_upgrade;

    proxy_redirect http://${TTTM_SPIDER_UPSTREAM}/ /tttm-spider/;
    proxy_redirect ~^(/.*)$ /tttm-spider$1;
  }

  # =========================
  # (C) 프론트엔드(SPA) 프록시 (그 외 모든 경로)
  # - ✅ 핵심: 여기서 Host를 반드시 $host로 "명시"해야
  #   Vite가 web_upstream Host로 차단하는 문제가 사라짐
  # =========================
  location / {
    proxy_pass http://web_upstream;

    # ✅ Vite allowedHosts 차단 방지 (필수)
    proxy_set_header Host $host;

    # WebSocket/HMR 업그레이드
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $connection_upgrade;

    # 원본 클라이언트 정보 전달
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;

    # (선택) 개발 서버 캐싱 방지 원하면
    # add_header Cache-Control "no-store";
  }
}
