apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: tailwind-local
resources:
  - keycloak.yaml
configMapGenerator:
  - name: keycloak-realm
    files:
      - realm-portal.json
